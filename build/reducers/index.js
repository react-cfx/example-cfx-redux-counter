// Generated by CoffeeScript 2.0.0-beta2
var count, dd, handleActions, initialState;

import {
  reduxActions
} from 'cfx.redux-actions';

({handleActions} = reduxActions);

import {
  mergeReduce
} from 'cfx.redux';

dd = require('ddeyes');

initialState = {
  count: 0
};

count = handleActions({
  INCREMENT: function(state, action) {
    action.payload || (action.payload = 1);
    return state + action.payload;
  },
  DECREMENT: function(state, action) {
    action.payload || (action.payload = 1);
    return state - action.payload;
  }
}, 0);

export default mergeReduce({count}, initialState);
