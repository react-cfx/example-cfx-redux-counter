// Generated by CoffeeScript 1.10.0
var DECREMENT, INCREMENT, constants, count, handleActions, initialState, mergeReduce, reduxActions, ref;

reduxActions = require('cfx.redux-actions').reduxActions;

handleActions = reduxActions.handleActions;

mergeReduce = require('cfx.redux').mergeReduce;

constants = require('../constants/index');

ref = constants.types, INCREMENT = ref.INCREMENT, DECREMENT = ref.DECREMENT;

initialState = {
  count: 0
};

count = handleActions({
  INCREMENT: function(state, action) {
    action.payload || (action.payload = 1);
    return state + action.payload;
  },
  DECREMENT: function(state, action) {
    action.payload || (action.payload = 1);
    return state - action.payload;
  }
});

module.exports = mergeReduce({
  count: count
}, initialState);
